#!/bin/sh

set -e

case "${IFACE}" in
    vbox*)
            ARGS="-d ${IFACE}"
        ;;
    *)
        exit 0
        ;;
esac

test -x /usr/lib/virtualbox/VBoxTunctl || exit 0

/usr/lib/virtualbox/VBoxTunctl $ARGS
