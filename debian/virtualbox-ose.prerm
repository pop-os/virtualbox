#!/bin/sh

set -e

if [ -x "/etc/init.d/virtualbox-ose" ] && [ "$1" = remove ]; then
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d virtualbox-ose stop || exit $?
	else
		/etc/init.d/virtualbox-ose stop || exit $?
	fi
fi

#DEBHELPER#
